<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:RetroMultiTools.Localization"
             x:Class="RetroMultiTools.Views.BatchHeaderFixerView">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <StackPanel Spacing="16" MaxWidth="600" HorizontalAlignment="Left">
        <TextBlock Text="{Binding [HeaderFixer_Title], Source={x:Static loc:LocalizationManager.Instance}}" FontSize="18" FontWeight="Bold" Foreground="#CDD6F4"/>
        <TextBlock Text="{Binding [HeaderFixer_Description], Source={x:Static loc:LocalizationManager.Instance}}"
                   Foreground="#A6ADC8" TextWrapping="Wrap"/>

        <!-- Mode Selection -->
        <StackPanel Spacing="4">
            <TextBlock Text="{Binding [Common_Mode], Source={x:Static loc:LocalizationManager.Instance}}" Foreground="#A6ADC8"/>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <RadioButton x:Name="SingleModeRadio" Content="{Binding [Common_SingleRom], Source={x:Static loc:LocalizationManager.Instance}}"
                             GroupName="BatchHeaderFixerMode"
                             IsChecked="True" Foreground="#CDD6F4"
                             IsCheckedChanged="ModeRadio_Checked"/>
                <RadioButton x:Name="BatchModeRadio" Content="{Binding [Common_BatchDirectory], Source={x:Static loc:LocalizationManager.Instance}}"
                             GroupName="BatchHeaderFixerMode"
                             Foreground="#CDD6F4"
                             IsCheckedChanged="ModeRadio_Checked"/>
            </StackPanel>
        </StackPanel>

        <!-- Input Selection -->
        <StackPanel Spacing="4">
            <TextBlock x:Name="InputLabel" Text="{Binding [Common_RomFile], Source={x:Static loc:LocalizationManager.Instance}}" Foreground="#A6ADC8"/>
            <Grid ColumnDefinitions="*,Auto">
                <TextBox x:Name="InputPathTextBox" IsReadOnly="True"
                         Watermark="{Binding [HeaderFixer_SelectFile], Source={x:Static loc:LocalizationManager.Instance}}" Padding="8,6"/>
                <Button Grid.Column="1" Content="{Binding [Common_Browse], Source={x:Static loc:LocalizationManager.Instance}}" Click="BrowseInput_Click"
                        Margin="8,0,0,0" Padding="12,6"/>
            </Grid>
        </StackPanel>

        <!-- Output -->
        <StackPanel Spacing="4">
            <TextBlock x:Name="OutputLabel" Text="{Binding [Common_OutputFile], Source={x:Static loc:LocalizationManager.Instance}}" Foreground="#A6ADC8"/>
            <Grid ColumnDefinitions="*,Auto">
                <TextBox x:Name="OutputPathTextBox" Watermark="{Binding [Common_OutputPath], Source={x:Static loc:LocalizationManager.Instance}}" Padding="8,6"/>
                <Button Grid.Column="1" Content="{Binding [Common_Browse], Source={x:Static loc:LocalizationManager.Instance}}" Click="BrowseOutput_Click"
                        Margin="8,0,0,0" Padding="12,6"/>
            </Grid>
        </StackPanel>

        <!-- Fix Button -->
        <Button x:Name="FixButton" Content="{Binding [HeaderFixer_FixHeaders], Source={x:Static loc:LocalizationManager.Instance}}"
                Click="FixButton_Click"
                Padding="16,10" HorizontalAlignment="Left"
                IsEnabled="False"/>

        <!-- Progress -->
        <StackPanel x:Name="ProgressPanel" IsVisible="False" Spacing="4">
            <ProgressBar IsIndeterminate="True" Height="4" Foreground="#89B4FA"/>
            <TextBlock x:Name="ProgressText" Foreground="#A6ADC8" FontSize="12"/>
        </StackPanel>

        <!-- Status -->
        <Border x:Name="StatusBorder" Background="#181825" CornerRadius="6" Padding="12"
                IsVisible="False">
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" Foreground="#CDD6F4"/>
        </Border>

        <!-- Details -->
        <Border x:Name="DetailsBorder" Background="#181825" CornerRadius="6" Padding="12"
                IsVisible="False" MaxHeight="200">
            <ScrollViewer>
                <TextBlock x:Name="DetailsText" Foreground="#A6ADC8" FontSize="12" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </StackPanel>
    </ScrollViewer>
</UserControl>
